version: '2'

services:
  mongo-cluster:
    image: mongo
    ports:
      - "27017:27017"
    command: "--smallfiles --logpath=/dev/null"
  embed-api:
    build: .
    ports:
      - "3050:3050"
    links:
      - mongo-cluster
    environment:
      PORT: 3050
